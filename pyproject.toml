[project]
name = "acafeed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "coverage>=7.10.7",
    "feedparser>=6.0.12",
    "pandas>=2.3.3",
]

[project.scripts]
acafeed = "acafeed.main:main"
